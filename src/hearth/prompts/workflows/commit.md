Write a git commit message for the staged changes below.

Output only the message: a subject line, then — only if the change needs explaining — a blank
line and a short body. No code fences, no quotation marks, no preamble.

The subject:

- is at most 72 characters and in the imperative ("Add", "Fix", "Rename"), not the past tense;
- says what the change *does*, not which files it touches;
- matches the style of the recent commits listed below when they show one.

The body, if there is one, explains **why** — what was wrong, or what the change makes
possible. The diff already shows what changed; do not narrate it line by line. Wrap at 72.

Describe only what the diff shows. If the diff does not tell you why, leave the why out
rather than inventing a motive.

{{omitted}}
--- recent commit subjects ---
{{log}}

--- staged changes ---
{{diff}}
